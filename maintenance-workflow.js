import { auth, db } from './firebase-config.js';
import { addDoc, collection, doc, getDoc, getDocs, runTransaction, setDoc, updateDoc } from 'https://www.gstatic.com/firebasejs/11.0.1/firebase-firestore.js';

const byId=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const cls='w-full bg-slate-950 border border-slate-700 rounded-lg px-3 py-2.5 text-sm';
const now=()=>new Date().toISOString();
const today=()=>now().slice(0,10);
const TYPES=['Inspection','Preventive Maintenance','Repair','Calibration','Certification / Recertification','Testing','Service','Other'];
const DEFAULT_TASKS={
  'Inspection':['Inspect condition','Test / verify','Record result'],
  'Preventive Maintenance':['Inspect condition','Service / replace consumables','Functional test'],
  'Repair':['Diagnose issue','Repair / replace','Functional test'],
  'Calibration':['Perform calibration','Verify tolerance','Record certificate / result'],
  'Certification / Recertification':['Inspect / test','Certification review','Record certificate'],
  'Testing':['Prepare test','Perform test','Record result'],
  'Service':['Inspect condition','Perform service','Functional test'],
  'Other':['Perform work','Verify result']
};
let inventory=[],settings=[],maintenance=[],me=null,busy=false,lastMount=null;

function balances(item){return (Array.isArray(item.stockBalances)?item.stockBalances:[]).filter(x=>Number(x.qty||0)>0).map(x=>({qty:Number(x.qty||0),locationType:x.locationType||'warehouse',locationId:x.locationId||'',locationName:x.locationName||x.location||'Unknown',status:x.status||''}));}
function maintBalances(item){return balances(item).filter(x=>x.locationType==='maintenance');}
function summary(b){const p=b.filter(x=>Number(x.qty)>0);if(!p.length)return{status:'Written Off',location:'No Stock'};if(p.length===1)return{status:p[0].status||'At Warehouse',location:p[0].locationName};return{status:'Multiple Locations',location:`${p.length} Locations`};}
function latestJob(itemId){return maintenance.filter(x=>x.itemId===itemId).sort((a,b)=>String(b.createdAt||'').localeCompare(String(a.createdAt||'')))[0]||null;}
function activeItems(){return inventory.filter(i=>maintBalances(i).length);}
async function load(){const u=auth.currentUser;if(!u)throw new Error('Sign in required.');const ps=await getDoc(doc(db,'users',u.uid));if(!ps.exists()||ps.data().status!=='active')throw new Error('Active user profile required.');me={...ps.data(),email:ps.data().email||u.email||''};const [a,b,c]=await Promise.all([getDocs(collection(db,'inventory')),getDocs(collection(db,'settings')),getDocs(collection(db,'maintenance_events'))]);inventory=a.docs.map(d=>({id:d.id,...d.data()}));settings=b.docs.map(d=>({id:d.id,...d.data()}));maintenance=c.docs.map(d=>({id:d.id,...d.data()}));}
async function log(rec){await addDoc(collection(db,'operational_logs'),{logVersion:2,date:now(),performedBy:me.email,performedByRole:me.role,...rec});}
async function audit(action,targetName,beforeValue,afterValue,remark='',targetId='',metadata={}){await addDoc(collection(db,'audit_traces'),{traceVersion:3,actionType:action,module:'Maintenance',targetType:'maintenance',targetName,targetId,summary:`${action.replace(/_/g,' ')}: ${targetName}`,beforeValue,afterValue,changedFields:['maintenance'],remark,metadata,performedBy:me.email,performedByRole:me.role,performedAt:now()});}

function hideMaintenanceFromMove(){
  document.querySelectorAll('#bmAction option[value="RETURN_MAINTENANCE"]').forEach(x=>x.remove());
  const hiddenIds=new Set(inventory.filter(i=>balances(i).every(b=>b.locationType==='maintenance'||b.locationType==='transit')).map(i=>i.id));
  document.querySelectorAll('.bmItem').forEach(sel=>[...sel.options].forEach(o=>{if(hiddenIds.has(o.value))o.remove();}));
}

function shellHtml(){const items=activeItems();return `<form id="maintenanceWorkflowForm" class="space-y-3"><div class="text-[11px] text-slate-500">Maintenance is work tracking, not movement. Open a job, tick each task with its date, complete the job, then release the item back to a warehouse.</div><label class="block text-xs text-slate-400">Item in Maintenance<select id="mwItem" class="${cls} mt-1"><option value="">-- Select item --</option>${items.map(i=>`<option value="${i.id}">${esc(i.itemCode)} — ${esc(i.name)} (${maintBalances(i).reduce((a,b)=>a+b.qty,0)} ${esc(i.unit||'')})</option>`).join('')}</select></label><div id="mwBody"></div></form>`;}
function openHtml(item){return `<div class="border border-slate-800 rounded-xl p-3 space-y-3"><div class="font-semibold text-sm">Open Maintenance Job</div><div class="grid sm:grid-cols-2 gap-2"><label class="text-xs text-slate-400">Maintenance Type<select id="mwType" class="${cls} mt-1">${TYPES.map(x=>`<option>${esc(x)}</option>`).join('')}</select></label><label class="text-xs text-slate-400">Start Date<input id="mwStart" type="date" value="${today()}" class="${cls} mt-1"></label></div><label class="text-xs text-slate-400">Provider<input id="mwProvider" class="${cls} mt-1" placeholder="Workshop / provider"></label><label class="text-xs text-slate-400">Tasks — one per line<textarea id="mwTasks" rows="4" class="${cls} mt-1">${DEFAULT_TASKS.Inspection.join('\n')}</textarea></label><label class="text-xs text-slate-400">Remark<textarea id="mwRemark" rows="2" class="${cls} mt-1"></textarea></label><button id="mwOpen" type="button" class="w-full bg-amber-600 hover:bg-amber-500 py-2.5 rounded-lg text-sm font-bold">Open Maintenance</button></div>`;}
function taskRows(job,readonly=false){const tasks=Array.isArray(job.tasks)?job.tasks:[];return tasks.map((t,n)=>`<div class="grid grid-cols-[auto_minmax(0,1fr)_145px] gap-2 items-center border border-slate-800 rounded-lg p-2"><input class="mwTaskDone" data-i="${n}" type="checkbox" ${t.done?'checked':''} ${readonly?'disabled':''}><div class="text-xs">${esc(t.name)}</div><input class="mwTaskDate ${cls}" data-i="${n}" type="date" value="${esc(t.date||'')}" ${readonly?'disabled':''}></div>`).join('')||'<div class="text-xs text-slate-500">No tasks.</div>';}
function jobHtml(item,job){const completed=job.eventStatus==='Completed';return `<div class="border border-slate-800 rounded-xl p-3 space-y-3"><div class="flex justify-between gap-3"><div><div class="font-semibold text-sm">${esc(job.eventType||'Maintenance')}</div><div class="text-[10px] text-slate-500">Opened ${esc(job.maintenanceFrom||job.eventDate||job.createdAt?.slice(0,10)||'')} · ${esc(job.provider||'No provider')}</div></div><span class="text-xs ${completed?'text-emerald-400':'text-amber-400'}">${completed?'Completed':'Open'}</span></div><div class="space-y-2">${taskRows(job,completed)}</div>${completed?`<div class="text-xs text-emerald-400">Completed on ${esc(job.completedDate||job.completedAt?.slice(0,10)||'—')}</div>${releaseHtml(item,job)}`:`<div class="grid sm:grid-cols-[1fr_auto] gap-2"><div class="text-[10px] text-slate-500 self-center">Tick a task and enter its completion date. Save progress whenever needed.</div><button id="mwSaveProgress" type="button" class="bg-slate-700 hover:bg-slate-600 px-4 py-2 rounded-lg text-xs font-bold">Save Progress</button></div><div class="border-t border-slate-800 pt-3 grid sm:grid-cols-[1fr_auto] gap-2"><label class="text-xs text-slate-400">Completion Date<input id="mwCompleteDate" type="date" value="${today()}" class="${cls} mt-1"></label><button id="mwComplete" type="button" class="bg-emerald-700 hover:bg-emerald-600 px-4 py-2 rounded-lg text-xs font-bold self-end">Complete Maintenance</button></div>`}</div>`;}
function releaseHtml(item,job){const mbs=maintBalances(item),warehouses=settings.filter(x=>x.type==='warehouse'&&x.status!=='inactive');return `<div class="border-t border-slate-800 pt-3 space-y-2"><div class="font-semibold text-xs text-cyan-300">Release / Return to Warehouse</div><div class="grid sm:grid-cols-2 gap-2"><label class="text-xs text-slate-400">Maintenance Source<select id="mwReleaseSource" class="${cls} mt-1">${mbs.map((b,n)=>`<option value="${n}">${esc(b.locationName)} — ${b.qty} ${esc(item.unit||'')}</option>`).join('')}</select></label><label class="text-xs text-slate-400">Warehouse<select id="mwWarehouse" class="${cls} mt-1"><option value="">-- Warehouse --</option>${warehouses.map(w=>`<option value="${w.id}" data-name="${esc(w.value)}">${esc(w.value)}</option>`).join('')}</select></label></div><div class="grid sm:grid-cols-2 gap-2"><label class="text-xs text-slate-400">Quantity<input id="mwReleaseQty" type="number" min="0.0001" step="any" value="${item.trackingType==='serialized'?1:(mbs[0]?.qty||1)}" ${item.trackingType==='serialized'?'readonly':''} class="${cls} mt-1"></label><label class="text-xs text-slate-400">Remark<input id="mwReleaseRemark" class="${cls} mt-1" placeholder="Optional release remark"></label></div><button id="mwRelease" type="button" class="w-full bg-cyan-700 hover:bg-cyan-600 py-2.5 rounded-lg text-sm font-bold">Start Return to Warehouse</button></div>`;}

async function renderSelected(){const body=byId('mwBody'),item=inventory.find(x=>x.id===byId('mwItem')?.value);if(!body)return;if(!item){body.innerHTML='';return;}const job=latestJob(item.id);body.innerHTML=job?jobHtml(item,job):openHtml(item);if(!job){byId('mwType').onchange=()=>{byId('mwTasks').value=(DEFAULT_TASKS[byId('mwType').value]||DEFAULT_TASKS.Other).join('\n');};byId('mwOpen').onclick=()=>openJob(item);}else if(job.eventStatus==='Completed'){byId('mwRelease').onclick=()=>release(item,job);}else{byId('mwSaveProgress').onclick=()=>saveProgress(item,job,false);byId('mwComplete').onclick=()=>saveProgress(item,job,true);}}
function collectTasks(job){return (Array.isArray(job.tasks)?job.tasks:[]).map((t,n)=>({name:t.name,done:document.querySelector(`.mwTaskDone[data-i="${n}"]`)?.checked||false,date:document.querySelector(`.mwTaskDate[data-i="${n}"]`)?.value||''}));}
async function openJob(item){if(busy)return;const tasks=byId('mwTasks').value.split('\n').map(x=>x.trim()).filter(Boolean);if(!tasks.length)return alert('Add at least one maintenance task.');const start=byId('mwStart').value;if(!start)return alert('Enter the maintenance start date.');busy=true;try{const ref=doc(collection(db,'maintenance_events')),rec={eventId:ref.id,itemId:item.id,itemCode:item.itemCode,itemNameSnapshot:item.name,eventType:byId('mwType').value,maintenanceType:byId('mwType').value,eventStatus:'Open',maintenanceFrom:start,provider:byId('mwProvider').value.trim(),remark:byId('mwRemark').value.trim(),tasks:tasks.map(name=>({name,done:false,date:''})),createdBy:me.email,createdByRole:me.role,createdAt:now()};await setDoc(ref,rec);await log({activity:'MAINTENANCE_OPEN',activityLabel:'Open Maintenance',status:'In Maintenance',fromType:'maintenance',fromName:maintBalances(item)[0]?.locationName||'Maintenance',toType:'maintenance',toName:maintBalances(item)[0]?.locationName||'Maintenance',qty:maintBalances(item).reduce((a,b)=>a+b.qty,0),unit:item.unit||'',itemId:item.id,itemCode:item.itemCode,itemName:item.name,category:item.category||'',supplierId:item.supplierId||'',supplierName:item.supplierName||'',clientId:'',clientName:'',remark:rec.remark});await audit('OPEN_MAINTENANCE',item.itemCode,null,{type:rec.eventType,start,tasks:rec.tasks.map(x=>x.name)},rec.remark,ref.id);await load();await renderSelected();}catch(err){alert('Open maintenance failed: '+(err?.message||err));}finally{busy=false;}}
async function saveProgress(item,job,complete){if(busy)return;const tasks=collectTasks(job);for(const t of tasks){if(t.done&&!t.date)return alert(`Enter a date for completed task: ${t.name}`);if(!t.done&&t.date)return alert(`Tick the task or clear its date: ${t.name}`);}if(complete&&!tasks.every(t=>t.done&&t.date))return alert('Complete every task with a date before completing maintenance.');const completedDate=complete?byId('mwCompleteDate').value:'';if(complete&&!completedDate)return alert('Enter the completion date.');busy=true;try{const patch={tasks,eventStatus:complete?'Completed':'Open',updatedAt:now(),updatedBy:me.email};if(complete)Object.assign(patch,{completedDate,completedAt:now(),completedBy:me.email});await updateDoc(doc(db,'maintenance_events',job.id),patch);await log({activity:complete?'MAINTENANCE_COMPLETE':'MAINTENANCE_PROGRESS',activityLabel:complete?'Complete Maintenance':'Maintenance Progress',status:'In Maintenance',fromType:'maintenance',fromName:maintBalances(item)[0]?.locationName||'Maintenance',toType:'maintenance',toName:maintBalances(item)[0]?.locationName||'Maintenance',qty:maintBalances(item).reduce((a,b)=>a+b.qty,0),unit:item.unit||'',itemId:item.id,itemCode:item.itemCode,itemName:item.name,category:item.category||'',supplierId:item.supplierId||'',supplierName:item.supplierName||'',clientId:'',clientName:'',remark:complete?`Completed ${completedDate}`:'Task progress updated'});await audit(complete?'COMPLETE_MAINTENANCE':'UPDATE_MAINTENANCE',item.itemCode,{status:job.eventStatus,tasks:job.tasks},{status:patch.eventStatus,tasks,completedDate},'',job.id);await load();await renderSelected();}catch(err){alert('Maintenance update failed: '+(err?.message||err));}finally{busy=false;}}
async function release(item,job){if(busy)return;const srcIndex=Number(byId('mwReleaseSource').value),src=maintBalances(item)[srcIndex],qty=Number(byId('mwReleaseQty').value||0),wh=byId('mwWarehouse'),toId=wh.value,toName=wh.selectedOptions?.[0]?.dataset?.name||'',remark=byId('mwReleaseRemark').value.trim();if(!src||qty<=0||qty>src.qty||!toId||!toName)return alert('Select a valid source, quantity and warehouse.');if(item.trackingType==='serialized'&&qty!==1)return alert('Serialized items must return as quantity 1.');busy=true;try{const movementRef=doc(collection(db,'movements')),movementId=movementRef.id,createdAt=now();await runTransaction(db,async tx=>{const itemRef=doc(db,'inventory',item.id),snap=await tx.get(itemRef);if(!snap.exists())throw new Error('Item missing.');const cur=snap.data(),bal=balances(cur),source=bal.find(b=>b.locationType==='maintenance'&&b.locationName===src.locationName&&(!src.locationId||b.locationId===src.locationId));if(!source||source.qty<qty)throw new Error('Maintenance stock changed. Refresh and retry.');source.qty-=qty;bal.push({qty,locationType:'transit',locationId:movementId,locationName:`Transit to ${toName}`,status:'In Transit'});const clean=bal.filter(b=>b.qty>0),sum=summary(clean);tx.update(itemRef,{stockBalances:clean,status:sum.status,currentLocation:sum.location,lastEditedBy:me.email,lastEditedAt:createdAt});tx.set(movementRef,{movementId,itemId:item.id,itemCode:item.itemCode,itemNameSnapshot:item.name,categorySnapshot:item.category||'',supplierId:item.supplierId||'',supplierNameSnapshot:item.supplierName||'',action:'RETURN_MAINTENANCE',actionLabel:'Return from Maintenance',fromType:'maintenance',fromId:src.locationId||'',fromName:src.locationName,toType:'warehouse',toId,toName,toStatus:'At Warehouse',qty,unit:item.unit||'',mode:'Maintenance Release',detail:`Completed maintenance job ${job.id}`,remark,documents:[],status:'in_transit',createdAt,createdBy:me.email,createdByRole:me.role});});await log({activity:'RETURN_MAINTENANCE',activityLabel:'Return from Maintenance',status:'In Transit',fromType:'maintenance',fromName:src.locationName,toType:'warehouse',toName,qty,unit:item.unit||'',itemId:item.id,itemCode:item.itemCode,itemName:item.name,category:item.category||'',supplierId:item.supplierId||'',supplierName:item.supplierName||'',clientId:'',clientName:'',movementId,remark});await audit('RETURN_MAINTENANCE',item.itemCode,{status:'In Maintenance',location:src.locationName},{status:'In Transit',destination:toName,qty},remark,movementId,{maintenanceEventId:job.id});alert('Return to warehouse started. Complete it in Active Transit / Arrival.');location.reload();}catch(err){alert('Maintenance release failed: '+(err?.message||err));}finally{busy=false;}}

async function mount(){const core=byId('maintenanceForm');if(!core||core.dataset.maintenanceWorkflow==='1'||core===lastMount)return;lastMount=core;try{await load();}catch(err){console.warn('IMS maintenance workflow unavailable:',err);return;}const replacement=document.createElement('div');replacement.innerHTML=shellHtml();const form=replacement.firstElementChild;form.dataset.maintenanceWorkflow='1';core.replaceWith(form);byId('mwItem').onchange=renderSelected;hideMaintenanceFromMove();}
let timer;new MutationObserver(()=>{clearTimeout(timer);timer=setTimeout(()=>{mount();hideMaintenanceFromMove();},35);}).observe(document.body,{childList:true,subtree:true});
mount();

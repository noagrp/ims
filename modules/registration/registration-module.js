import {auth,db} from '../../firebase-config.js';
import {addDoc,collection,doc,getDoc,getDocs,limit,query,runTransaction,setDoc,where} from 'https://www.gstatic.com/firebasejs/11.0.1/firebase-firestore.js';
import {can} from '../../ims-permissions.js';
import {applyInventorySummaryDelta,inventorySummaryDelta} from '../inventory/inventory-summary.js';

const $=id=>document.getElementById(id);
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const norm=s=>String(s??'').trim().replace(/\s+/g,' ').toLowerCase();
const now=()=>new Date().toISOString();
const cls='w-full min-w-0 bg-slate-950 border border-slate-700 rounded-lg px-3 py-2.5 text-sm';
let settings=[],suppliers=[],saving=false,batchSession=null;
function roles(x){return Array.isArray(x.businessRoles)?x.businessRoles:[];}
function isSupplier(x){const r=roles(x);return r.length?r.includes('supplier'):x.sourceKind==='supplier';}
function businessName(x){return x?.companyName||x?.supplierName||x?.clientName||'';}
async function load(){const[a,b,c]=await Promise.all(['settings','supplier_profiles','client_profiles'].map(n=>getDocs(collection(db,n))));settings=a.docs.map(x=>({id:x.id,...x.data()}));suppliers=[...b.docs.map(x=>({id:x.id,sourceKind:'supplier',...x.data()})),...c.docs.map(x=>({id:x.id,sourceKind:'client',...x.data()}))].filter(x=>x.status!=='inactive'&&isSupplier(x));}
function active(type){return settings.filter(x=>x.type===type&&x.status!=='inactive').sort((a,b)=>String(a.value||'').localeCompare(String(b.value||''),undefined,{numeric:true,sensitivity:'base'}));}
function opts(type,label){return`<option value="">-- ${label} --</option>${active(type).map(x=>`<option value="${x.id}" data-v="${esc(x.value)}">${esc(x.value)}</option>`).join('')}`;}
function sel(id){const e=$(id),o=e?.selectedOptions?.[0];return{id:e?.value||'',value:o?.dataset?.v||o?.textContent?.trim()||''};}
function itemCode(ref){return`ITM-${ref.id.slice(0,10).toUpperCase()}`;}
function lines(id,count,label,required=false){const all=($(id)?.value||'').split('\n').map(x=>x.trim());if(count===1){if(required&&!all[0])throw new Error(`${label} required.`);return[all[0]||''];}const entered=all.filter(Boolean);if(required&&entered.length!==count)throw new Error(`Enter exactly ${count} ${label} values, one per unit.`);if(!required&&entered.length&&entered.length!==count)throw new Error(`Enter either no ${label} values or exactly ${count}.`);return entered.length?all:Array(count).fill('');}
async function assertAliasesUnique(aliases,label='Wellora SN'){const seen=new Set((batchSession?.aliases||[]).map(norm).filter(Boolean));for(const a of aliases){const k=norm(a);if(seen.has(k))throw new Error(`${label} already exists in this batch: ${a}`);seen.add(k);const snap=await getDocs(query(collection(db,'inventory'),where('alias','==',a),limit(1)));if(!snap.empty)throw new Error(`${label} already exists: ${a}`);}}
function section(title,body){return`<section class="border border-slate-800 rounded-2xl p-4"><div class="text-xs font-bold uppercase text-slate-500 mb-3">${title}</div><div style="display:grid;grid-template-columns:repeat(auto-fit,minmax(190px,1fr));gap:.75rem;align-items:end">${body}</div></section>`;}
function field(label,body,style=''){return`<label class="block min-w-0 text-xs text-slate-400"${style?` style="${style}"`:''}>${label}${body}</label>`;}
function locationHtml(){const atSupplier=$('regPosition')?.value==='supplier',supplier=suppliers.find(x=>x.id===$('regSupplier')?.value),wrap=$('regLocationWrap');if(!wrap)return;wrap.innerHTML=atSupplier?field('Initial Supplier Location',`<input id="regLocation" readonly class="${cls} mt-1" value="${esc(businessName(supplier)||'Select supplier')}">`):field('Initial Warehouse / Location',`<select id="regLocation" required class="${cls} mt-1">${opts('warehouse','Warehouse / Location')}</select>`);}
function applyType(){const q=$('regQty'),type=active('item_type').find(x=>x.id===$('regType')?.value);if(!q)return;if(type?.quantityMode==='single'){q.value='1';q.disabled=true;}else{q.disabled=false;if(type?.defaultQty&&!batchSession)q.value=String(Math.max(1,Number(type.defaultQty)||1));}}
function sessionBanner(){if(!batchSession)return'<div class="text-xs text-slate-500">No active batch. First Add Item(s) starts one.</div>';return`<div class="rounded-xl border border-emerald-900/50 p-3 text-xs"><b class="text-emerald-300">Open Batch ${esc(batchSession.id)}</b><div class="text-slate-400 mt-1">Wellora PO ${esc(batchSession.po)} · ${batchSession.count} item(s) added. Add more Wellora SN / COC / Mill No, then click Done.</div></div>`;}
function html(){return`<form id="registerForm" data-ims-registration-module="3" class="space-y-4"><div class="rounded-xl border border-cyan-900/50 p-3 text-xs"><b class="text-cyan-300">Ownership: Owned</b><div class="text-slate-500 mt-1">Add one or many items into an open batch. Shared fields stay on screen; finish every session with Done.</div></div><div id="regSession">${sessionBanner()}</div>${section('Quantity / Identity',`${field('Item Type',`<select id="regType" required class="${cls} mt-1">${opts('item_type','Item Type')}</select>`)}${field('Qty',`<input id="regQty" type="number" min="1" max="500" step="1" value="1" class="${cls} mt-1">`)}${field('Unit',`<select id="regUnit" required class="${cls} mt-1">${opts('unit','Unit')}</select>`)}${field('Description',`<input id="regName" required class="${cls} mt-1">`,'grid-column:span 2')}${field('Wellora SN',`<textarea id="regAliases" rows="2" required class="${cls} mt-1" placeholder="One Wellora SN per unit"></textarea>`)}${field('COC / Mill No',`<textarea id="regCOC" rows="2" class="${cls} mt-1" placeholder="One COC / Mill No per unit if used"></textarea>`)}`)}${section('Classification / Specification',`${field('Category',`<select id="regCategory" required class="${cls} mt-1">${opts('category','Category')}</select>`)}${field('Size',`<select id="regSize" class="${cls} mt-1">${opts('size','Size')}</select>`)}${field('Grade',`<select id="regGrade" class="${cls} mt-1">${opts('grade','Grade')}</select>`)}${field('PPF',`<select id="regPpf" class="${cls} mt-1">${opts('ppf','PPF')}</select>`)}${field('Connection',`<select id="regConnection" class="${cls} mt-1">${opts('connection','Connection')}</select>`)}${field('Range',`<select id="regRange" class="${cls} mt-1">${opts('range','Range')}</select>`)}${field('Brand',`<select id="regBrand" class="${cls} mt-1">${opts('brand','Brand')}</select>`)}${field('Model',`<select id="regModel" class="${cls} mt-1">${opts('model','Model')}</select>`)}${field('Weight',`<input id="regWeight" type="number" min="0" step="any" class="${cls} mt-1">`)}${field('Weight Unit',`<select id="regWeightUnit" class="${cls} mt-1">${opts('weight_unit','Weight Unit')}</select>`)}`)}${section('Supplier / PO / Initial Position',`${field('Supplier',`<select id="regSupplier" required class="${cls} mt-1"><option value="">-- Supplier --</option>${suppliers.map(x=>`<option value="${x.id}">${esc(businessName(x))}</option>`).join('')}</select>`)}${field('Wellora PO',`<input id="regPO" required class="${cls} mt-1">`)}${field('PO Total Amount',`<input id="regPOAmount" type="number" min="0" step="0.01" class="${cls} mt-1">`)}${field('Currency',`<input id="regCurrency" value="MYR" class="${cls} mt-1">`)}${field('Initial Position',`<select id="regPosition" class="${cls} mt-1"><option value="warehouse">Available at Warehouse</option><option value="supplier">At Supplier</option></select>`)}<div id="regLocationWrap"></div>${field('Delivery Ticket / DO',`<input id="regDO" class="${cls} mt-1">`)}${field('Receiving Ticket',`<input id="regReceiving" class="${cls} mt-1">`)}`)}${field('Remark',`<textarea id="regRemark" rows="2" class="${cls} mt-1"></textarea>`)}<div class="grid sm:grid-cols-2 gap-2"><button id="regSave" class="bg-red-600 py-3 rounded-lg text-sm font-bold">Add Item(s) to Batch</button><button id="regDone" type="button" class="bg-emerald-700 py-3 rounded-lg text-sm font-bold">Done</button></div><div id="regResult"></div></form>`;}
function lockShared(on){['regType','regUnit','regName','regCategory','regSize','regGrade','regPpf','regConnection','regRange','regBrand','regModel','regWeight','regWeightUnit','regSupplier','regPO','regPOAmount','regCurrency','regPosition','regLocation','regDO','regReceiving','regRemark'].forEach(id=>{const e=$(id);if(e)e.disabled=on;});}
function clearIdentity(){['regAliases','regCOC'].forEach(id=>{if($(id))$(id).value='';});if($('regQty')&&!$('regQty').disabled)$('regQty').value='1';$('regAliases')?.focus();}
async function createInventoryItem(ref,rec,email,at){await runTransaction(db,async tx=>{const existing=await tx.get(ref);if(existing.exists())throw new Error(`Inventory ID collision: ${rec.itemCode}`);await applyInventorySummaryDelta(tx,inventorySummaryDelta({},rec),{updatedAt:at,updatedBy:email});tx.set(ref,rec);});}
async function save(e){e.preventDefault();if(saving||!can('inventory.add'))return;saving=true;const btn=$('regSave');btn.disabled=true;try{await load();const u=auth.currentUser,p=await getDoc(doc(db,'users',u.uid));if(!p.exists()||p.data().status!=='active')throw new Error('Active user required.');const user=p.data(),email=user.email||u.email||'',type=sel('regType'),typeRec=active('item_type').find(x=>x.id===type.id),unit=sel('regUnit'),cat=sel('regCategory'),description=$('regName').value.trim(),supplier=suppliers.find(x=>x.id===$('regSupplier').value),po=$('regPO').value.trim(),poAmount=$('regPOAmount').value===''?null:Number($('regPOAmount').value),currency=$('regCurrency').value.trim()||'MYR',position=$('regPosition').value;let qty=Number($('regQty').value);if(typeRec?.quantityMode==='single')qty=1;if(!Number.isInteger(qty)||qty<1||qty>500)throw new Error('Qty must be 1-500.');if(!type.value||!unit.value||!cat.value||!description||!supplier||!po)throw new Error('Complete Item Type, Unit, Description, Category, Supplier and Wellora PO.');if(batchSession&&batchSession.po!==po)throw new Error('Finish the current batch before changing Wellora PO.');if(poAmount!==null&&(!Number.isFinite(poAmount)||poAmount<0))throw new Error('PO Total Amount must be valid.');const weight=$('regWeight').value===''?null:Number($('regWeight').value),weightUnit=sel('regWeightUnit');if(weight!==null&&(!Number.isFinite(weight)||weight<0))throw new Error('Weight must be valid.');if(weight!==null&&!weightUnit.value)throw new Error('Select Weight Unit when Weight is entered.');const aliases=lines('regAliases',qty,'Wellora SN',true),cocs=lines('regCOC',qty,'COC / Mill No');await assertAliasesUnique(aliases,'Wellora SN');const loc=position==='supplier'?{id:supplier.id,value:businessName(supplier)}:sel('regLocation');if(!loc.value)throw new Error('Select initial location.');const status=position==='supplier'?'At Supplier':'Available',at=now(),size=sel('regSize'),grade=sel('regGrade'),ppf=sel('regPpf'),connection=sel('regConnection'),range=sel('regRange'),brand=sel('regBrand'),model=sel('regModel'),deliveryTicket=$('regDO').value.trim(),receivingTicket=$('regReceiving').value.trim(),remark=$('regRemark').value.trim();if(!batchSession){const batch=doc(collection(db,'registration_batches'));batchSession={id:batch.id,ref:batch,po,count:0,itemIds:[],itemCodes:[],aliases:[],createdAt:at,createdBy:email,itemName:description,supplierName:businessName(supplier)};await setDoc(batch,{batchId:batch.id,ownershipType:'owned',quantity:qty,itemType:type.value,itemName:description,category:cat.value,unit:unit.value,supplierId:supplier.id,supplierName:businessName(supplier),ourPONumber:po,ourPOAmount:poAmount,currency,initialPosition:position,initialLocation:loc.value,deliveryTicket,receivingTicket,createdAt:at,createdBy:email,completionStatus:'open',completedCount:0,itemIds:[],itemCodes:[],aliases:[]});lockShared(true);}const created=[];for(let n=0;n<qty;n++){const r=doc(collection(db,'inventory')),index=batchSession.count+n+1,rec={itemCode:itemCode(r),alias:aliases[n],name:description,coc:cocs[n]||'',type:type.value,itemTypeId:type.id,category:cat.value,categoryId:cat.id,size:size.value,sizeId:size.id,grade:grade.value,gradeId:grade.id,ppf:ppf.value,ppfId:ppf.id,connection:connection.value,connectionId:connection.id,range:range.value,rangeId:range.id,brand:brand.value,brandId:brand.id,model:model.value,modelId:model.id,weight,weightUnit:weightUnit.value,weightUnitId:weightUnit.id,quantity:1,unit:unit.value,unitId:unit.id,ownershipType:'owned',ownershipLabel:'Owned',sourceModule:'Registration',supplierId:supplier.id,supplierName:businessName(supplier),ourPONumber:po,ourPOAmount:poAmount,currency,status,currentLocation:loc.value,stockBalances:[{qty:1,locationType:position,locationId:loc.id,locationName:loc.value,status}],deliveryTicket,receivingTicket,remark,registrationBatchId:batchSession.id,registrationBatchIndex:index,createdAt:at,createdBy:email,lastEditedAt:null,lastEditedBy:null};await createInventoryItem(r,rec,email,at);await addDoc(collection(db,'operational_logs'),{date:at,activity:'REGISTER_ITEM',activityLabel:'Register Owned Item',itemId:r.id,itemCode:rec.itemCode,itemAlias:rec.alias,itemName:description,status,toName:loc.value,qty:1,unit:unit.value,supplierName:businessName(supplier),ourPONumber:po,performedBy:email,remark});created.push({id:r.id,...rec});}batchSession.count+=created.length;batchSession.itemIds.push(...created.map(x=>x.id));batchSession.itemCodes.push(...created.map(x=>x.itemCode));batchSession.aliases.push(...created.map(x=>x.alias));await setDoc(batchSession.ref,{quantity:batchSession.count,completedCount:batchSession.count,itemIds:batchSession.itemIds,itemCodes:batchSession.itemCodes,aliases:batchSession.aliases,lastAddedAt:now()},{merge:true});$('regSession').innerHTML=sessionBanner();$('regResult').innerHTML=`<div class="border border-emerald-800 rounded-xl p-3 text-xs"><b class="text-emerald-300">Added ${created.length} item(s). Batch remains open.</b><div class="mt-2">${created.map(i=>`<span class="text-cyan-300 mr-2">${esc(i.alias)}</span>`).join('')}</div></div>`;clearIdentity();}catch(err){alert('Registration failed: '+(err?.message||err));}finally{saving=false;btn.disabled=false;}}
async function done(){if(!batchSession)return alert('No open batch to complete.');const s=batchSession;await setDoc(s.ref,{completionStatus:'completed',completedCount:s.count,quantity:s.count,itemIds:s.itemIds,itemCodes:s.itemCodes,aliases:s.aliases,completedAt:now(),completedBy:auth.currentUser?.email||''},{merge:true});await addDoc(collection(db,'audit_traces'),{traceVersion:3,actionType:'REGISTER_ITEM_BATCH',module:'Registration',targetType:'registration_batch',targetName:`${s.itemName} × ${s.count}`,targetId:s.id,summary:`Complete Registration Batch: ${s.itemName} × ${s.count}`,beforeValue:{completionStatus:'open'},afterValue:{completionStatus:'completed',quantity:s.count,ourPONumber:s.po,supplierName:s.supplierName,aliases:s.aliases},changedFields:['completionStatus','quantity','inventory'],remark:'',performedBy:auth.currentUser?.email||'',performedByRole:window.IMS_ROLE||'',performedAt:now()});batchSession=null;alert('Registration batch completed.');window.IMSWorkspace?.show?.('register');}
async function mount(){if(!can('inventory.add'))return;const core=$('registerForm');if(!core||core.dataset.imsRegistrationModule==='3')return;try{await load();}catch(err){console.error('IMS Registration reference-data load failed:',err);core.innerHTML=`<div class="border border-red-900 rounded-xl p-3 text-xs text-red-300">Unable to load Registration: ${esc(err?.message||err)}</div>`;throw err;}const w=document.createElement('div');w.innerHTML=html();core.replaceWith(w.firstElementChild);$('regType').onchange=applyType;$('regPosition').onchange=locationHtml;$('regSupplier').onchange=()=>{if($('regPosition').value==='supplier')locationHtml();};$('registerForm').onsubmit=save;$('regDone').onclick=done;locationHtml();applyType();if(batchSession){lockShared(true);$('regSession').innerHTML=sessionBanner();}}
let t;new MutationObserver(()=>{clearTimeout(t);t=setTimeout(()=>mount().catch(()=>{}),30)}).observe(document.body,{childList:true,subtree:true});mount().catch(()=>{});window.IMSRegistration=Object.freeze({mount,loadReferenceData:load,done});window.dispatchEvent(new CustomEvent('ims:registration-ready'));export{mount,load,done};